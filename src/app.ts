import express from 'express';
// import { explode, explodeAfterResponse } from './error';
const app = express.Router();

// TODO: set application router here
// app.use('/failOk', explodeAfterResponse());
// app.use('/fail', explode());
export default app;
